[Definition]
# Matches: pamsignal: BRUTE_FORCE_DETECTED ip=192.168.1.100 attempts=5 window=300s user=root
failregex = pamsignal: BRUTE_FORCE_DETECTED ip=<HOST> attempts=\d+ window=\d+s user=.*
ignoreregex = 
